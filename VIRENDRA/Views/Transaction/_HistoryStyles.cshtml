<style>
    .history-page {
        color: #111827;
    }

    .history-page .page-title-box {
        margin-bottom: 2rem;
    }

    .history-filter .form-control,
    .history-filter .form-select {
        border: 0;
        border-radius: 4px;
        height: 34px;
        background-color: #fff;
        box-shadow: none;
        font-size: 14px;
    }

    .btn-history-search {
        height: 34px;
        border: 0;
        border-radius: 4px;
        color: #fff;
        background: #74798e;
        box-shadow: 0 2px 6px rgba(41, 47, 66, 0.25);
        font-size: 14px;
    }

    .btn-history-reset {
        height: 34px;
        border: 1px solid #ff4d4f;
        border-radius: 4px;
        color: #ff4d4f;
        background: transparent;
        font-size: 14px;
        line-height: 20px;
    }

    .history-divider {
        margin: 18px 0;
        border-color: #b9bdc8;
        opacity: 1;
    }

    .history-table-wrap {
        background: #fff;
    }

    .history-table {
        background: #fff;
        font-size: 15px;
    }

    .history-table th {
        padding: 16px 14px;
        font-weight: 700;
        white-space: nowrap;
        border-bottom: 1px solid #e1e5ec;
    }

    .history-table td {
        padding: 16px 14px;
        vertical-align: middle;
        white-space: nowrap;
        border-bottom: 1px solid #e1e5ec;
    }

    .history-table tfoot td {
        font-weight: 500;
        border-bottom: 0;
    }

    .status-badge,
    .type-badge {
        border-radius: 4px;
        display: inline-block;
        font-size: 12px;
        font-weight: 700;
        min-width: 72px;
        padding: 4px 8px;
        text-align: center;
    }

    .status-success,
    .type-credit,
    .type-commission,
    .type-refund {
        background: #dcfce7;
        color: #16803b;
    }

    .status-pending {
        background: #fef3c7;
        color: #a16207;
    }

    .status-failed,
    .type-debit {
        background: #fee2e2;
        color: #dc2626;
    }
</style>
