@model VIRENDRA.Models.ApiSource

@{
    ViewBag.Title = "Add API Source";
    Layout = "~/Views/Shared/_LayoutDashboard.cshtml";
}

<div class="container-fluid">

    <!-- Page Title -->
    <div class="row mb-4">
        <div class="col-12">
            <div class="d-flex align-items-center justify-content-between flex-wrap gap-2">
                <div>
                    <h4 class="mb-1 fw-bold text-dark">
                        <i class="fas fa-plus-circle text-success me-2"></i>Add New API Source
                    </h4>
                    <nav aria-label="breadcrumb">
                        <ol class="breadcrumb mb-0" style="font-size:.82rem;">
                            <li class="breadcrumb-item"><a href="@Url.Action("Index", "Home")">Dashboard</a></li>
                            <li class="breadcrumb-item"><a href="@Url.Action("Index")">API Sources</a></li>
                            <li class="breadcrumb-item active">Add New</li>
                        </ol>
                    </nav>
                </div>
                <a href="@Url.Action("Index")" class="btn btn-outline-secondary btn-sm px-3">
                    <i class="fas fa-arrow-left me-1"></i>Back to List
                </a>
            </div>
        </div>
    </div>

    @using (Html.BeginForm("Create", "Vendor", FormMethod.Post))
    {
        @Html.AntiForgeryToken()
        @Html.HiddenFor(m => m.Id)

        @if (!ViewData.ModelState.IsValid)
        {
            <div class="alert alert-danger alert-dismissible mb-3" style="border-radius:10px;">
                <i class="fas fa-exclamation-circle me-2"></i>
                <strong>Please fix the errors below before saving.</strong>
                @Html.ValidationSummary(true, "", new { @class = "mb-0 mt-1" })
                <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
            </div>
        }

        @Html.Partial("_VendorForm", Model)

        <!-- Action Buttons -->
        <div class="row mt-4">
            <div class="col-12">
                <div class="card border-0 shadow-sm" style="border-radius:10px;">
                    <div class="card-body py-3 px-4 d-flex justify-content-between align-items-center flex-wrap gap-2">
                        <span class="text-muted small">
                            <i class="fas fa-info-circle me-1"></i>
                            All fields marked <span class="text-danger fw-bold">*</span> are required.
                        </span>
                        <div class="d-flex gap-2">
                            <a href="@Url.Action("Index")" class="btn btn-outline-secondary px-4">
                                <i class="fas fa-times me-1"></i>Cancel
                            </a>
                            <button type="submit" class="btn btn-success px-4 fw-semibold">
                                <i class="fas fa-save me-1"></i>Save API Source
                            </button>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    }
</div>
